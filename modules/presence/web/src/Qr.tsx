/**
 * Moved to packages/web-kit on issue 119, next to the `.qr-block` styles
 * that were already there. Re-exported so the presence call sites keep
 * working; import from @makerbay/web-kit in new code.
 */
export { QrBlock as default } from '@makerbay/web-kit'
