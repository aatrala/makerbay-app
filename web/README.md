# MakerBay Dashboard

React + Vite SPA served from S3 + CloudFront at `app.makerbay.app`.

The shell renders navigation from the tenant's entitlements — module UIs
(`modules/<name>/web`) are lazy-loaded sections that appear only when the
module is enabled. Scaffolded at milestone M2.
