{
  "//": "Lambda code ONLY. CDK's esbuild does not typecheck, so without this a type error ships and is found at runtime. This deliberately excludes the front end, which needs different lib and jsx settings - `npm run typecheck` runs this AND the two UI projects, because a check that silently skips every screen is worse than no check.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "bundler",
    "noEmit": true,
    "declaration": false,
    "sourceMap": false,
    "noUnusedParameters": true,
    "types": ["node"]
  },
  "include": ["modules/*/api/src/**/*.ts", "packages/*/src/**/*.ts"],
  "exclude": ["packages/web-kit/**", "**/node_modules/**"]
}
