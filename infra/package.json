{
  "name": "@makerbay/infra",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc --noEmit",
    "synth": "cdk synth --profile makerbay",
    "deploy": "cdk deploy --profile makerbay",
    "diff": "cdk diff --profile makerbay"
  },
  "dependencies": {
    "@makerbay/email": "1.0.0",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.170.0",
    "esbuild": "^0.25.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
