{
  "name": "makerbay",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "admin",
    "infra",
    "packages/*",
    "modules/*/api",
    "modules/*/embed",
    "modules/*/web",
    "site",
    "web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.check.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
